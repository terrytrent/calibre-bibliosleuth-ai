target-version = "py311"

[lint]
# Block syntax errors, invalid future annotations, broken control flow, and
# undefined names. Broader style/maintainability findings remain visible in the
# Qlty report while the existing codebase is improved incrementally.
select = ["E9", "F63", "F7", "F82"]
